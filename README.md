This project implements and benchmarks a cosine approximation using the Taylor series written in x86 (IA-32) assembly, with execution time measured in CPU cycles using the RDTSC instruction.

### Build & Run
```bash
make
